$('#<%= @div_id %>').replaceWith('<%= escape_javascript(render "pretty_calendar_month",
                                                            :calendar => @calendar,
                                                            :year => @year,
                                                            :month => @month,
                                                            :options => { :div_id => @div_id, :edit => @edit })%>')